type SelfUser {
  id: ID!
  username: String!
}

type Query {
  getSelf: SelfUser
}

type Mutation {
  signup(username: String!, password: String!): SelfUser!
  login(username: String!, password: String!): SelfUser!
}
